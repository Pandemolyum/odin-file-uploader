<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>

<body>
    <%- include("partials/header.ejs") %>

    <% if (isConnected) { %>
    <div class="greeting">Welcome!</div>
    <% } else { %>
    <div class="directions"><a href="/login">Login</a> to upload files or enter a sharing link.</div>
    <% } %>
</body>

</html>